# SEO Fixes — 2026-07-14 (Claude Code)

Repo: `sidingdepot-site` · branch: `main` · **tudo no working tree, NADA commitado/deployado.**
Typecheck: `tsc --noEmit` = **0 erros**. Verificado localmente via SSR (`http://localhost:8080`).

---

## ✅ Arquivos que EU alterei (revisar/commitar estes)

### 1. og:image quebrado → corrigido
Homepage apontava para `sidingdepot.vercel.app/og-default.jpg` (404). Demais páginas usavam `og-default.webp` (também 404 no ar). Tudo padronizado para `https://www.sidingdepot.com/og-default.jpg` (200).
- `src/routes/index.tsx` — og:image, og:image:secure_url, twitter:image (era vercel.app)
- `src/routes/__root.tsx` — og:image, twitter:image (era .webp)
- `.webp → .jpg` também em: `src/routes/siding.tsx`, `roofing.tsx`, `painting.tsx`, `projects.tsx`, `windows.tsx`, `doors.tsx`, `decks.tsx`, `gutters.tsx`, `dumpster-rental.tsx`, `blog.index.tsx`, e `src/components/site/SidingLP.tsx`

### 2. Reviews — schema alinhado à faixa visível (REGRA FIXA: 4.7 / 550)
Schema dizia 4.5/166 e contradizia a página (4.7/550). Alinhado a 4.7/550. Widget do Google mantém 166 (correto por plataforma).
- `src/routes/__root.tsx` — aggregateRating do nó LocalBusiness → 4.7 / 550
- `src/lib/jsonld.ts` — aggregateRating (páginas de serviço) → 4.7 / 550

### 3. Blog — NÃO reativado (revertido a pedido do usuário)
Blog permanece desabilitado no nav (`disabled: true`) e no rodapé (`<span>`). `Navbar.tsx` e `Footer.tsx` NÃO fazem parte deste commit.

### 4. Schema @type → GeneralContractor (mais específico; fator de ranking local)
- `src/lib/schema.ts` — `["HomeAndConstructionBusiness", "GeneralContractor"]`
- `src/routes/__root.tsx` — mesmo no nó aggregateRating
- `src/lib/jsonld.ts` — `["LocalBusiness", "GeneralContractor"]`

### 5. Performance / técnico
- `vercel.json` — adicionados security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS+includeSubDomains). **Só aparecem após deploy** (não no dev local).
- `src/routes/__root.tsx` — Metricool deferido (saiu do `<head>` inline → injeta no `useEffect` após hidratação); removido preconnect auto-referente (no-op).

### 6. Sitemap / robots / crawlers
- `scripts/generate-sitemap.ts` — removidos `/guide/thank-you` (noindex) e `/dumpster` (não-canônico)
- `public/sitemap.xml` + `public/blog-sitemap.xml` — **REGERADOS** por esse gerador (111 → 109 URLs; só saíram os 2 acima). Backup do estado anterior em `/tmp/sitemap.before.xml` e `/tmp/blog-sitemap.before.xml`.
- `public/robots.txt` — removido `Allow: /locations/` redundante
- `public/llms.txt` — corrigido link `/deck` → `/decks` (o arquivo já existia; estava só sem deploy)

### 7. Cosmético
- `src/components/site/EliteBadgeSection.tsx` — "ProtectYour" → "Protect Your" (espaço p/ extração de texto)

---

## ⛔ Arquivos que NÃO são meus (seu trabalho anterior — não commitei, deixe como está / trate à parte)
`src/components/blog/BlogPostDetail.tsx`, `src/components/site/HeroQuoteForm.tsx`, `src/data/blog-posts.ts`, `src/hooks/use-lead-form.ts`, `src/hooks/useSiteAnalytics.ts`, `src/lib/dashboard.functions.ts`, `src/routes/admin.analytics.tsx`, `src/routes/admin.login.tsx`, `src/routes/blog.$slug.tsx`, `src/routes/lp.siding-alpharetta.tsx`, `src/routes/seo-dashboard.tsx`, `vite.config.ts`, `supabase/.temp/cli-latest`.

---

## Como revisar / operar no Claude IDE
1. Abra o repo `sidingdepot-site` no IDE.
2. Painel **Source Control** → veja o diff de cada arquivo da lista "que eu alterei".
3. Para commitar SÓ os meus SEM misturar seu trabalho, faça **stage seletivo** dos arquivos das seções 1–7.
4. Deploy: siga suas regras do `CLAUDE.md` (checklist + `scripts/deploy-vercel.sh`). O `bun run build` vai regenerar os sitemaps de novo (idempotente).

## Ainda pendente (precisa de conteúdo/decisão — não automatizável)
- 80 páginas `/locations/*` com 96–98% de conteúdo duplicado (risco de doorway) → conteúdo único por cidade
- Seção de preço, membros nomeados + nº de licença visível, widget de review ao vivo

## Não relacionado (verificado a pedido)
Dashboard: use **`https://app.sidingdepot.com/admin/login`** (SEM `www.`). `www.app.sidingdepot.com` não tem DNS — por isso não abre. O dashboard em si está 200/saudável.
